twist_mux:
  ros__parameters:
    topics:
      # navigation:
      #   topic: /cmd_vel_nav
      #   priority: 10
      #   timeout: 0.5
      # navigation:
      #   topic: /cmd_vel_smoothed
      #   priority: 10
      #   timeout: 0.5
      navigation:
        topic: /cmd_vel_collision
        priority: 10
        timeout: 0.5
      teleop:
        topic: /cmd_vel_teleop
        priority: 20
        timeout: 0.5
    use_stamped: true
